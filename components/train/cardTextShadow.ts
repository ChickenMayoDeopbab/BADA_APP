import { TextStyle } from "react-native";

/** 이미지 위에 올라가는 카드 텍스트의 그림자 (디자인: 0 0 3.9px rgba(0,0,0,0.25)) */
export const CARD_TEXT_SHADOW: TextStyle = {
  textShadowColor: "rgba(0,0,0,0.25)",
  textShadowOffset: { width: 0, height: 0 },
  textShadowRadius: 3.9,
};
